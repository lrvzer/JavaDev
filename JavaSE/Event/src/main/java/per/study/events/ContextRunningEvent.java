package per.study.events;

/**
 * ContextRunningEvent事件是AbstractContextEvent具体实现类，容器启动后时触发，
 * 这里为了demo简单，这里不再定义任何事件逻辑，只是代表容器启动运行时时的一个标志事件类。
 **/
public class ContextRunningEvent extends AbstractContextEvent {
}

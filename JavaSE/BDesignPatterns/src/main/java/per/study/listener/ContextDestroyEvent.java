package per.study.listener;

/**
 * ContextDestroyEvent事件是AbstractContextEvent具体实现类，容器销毁时触发，
 * 这里为了demo简单，这里不再定义任何事件逻辑，只是代表容器销毁时的一个标志事件类。
 **/
public class ContextDestroyEvent extends AbstractContextEvent {
}
